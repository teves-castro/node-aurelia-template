// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "node_modules": true,
        "**/jspm_packages": true,
        "**/*.js": {
            "when": "$(basename).ts"
        }
    }
}